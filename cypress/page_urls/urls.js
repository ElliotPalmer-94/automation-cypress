// ================= URL =================
const entryAdd = `entry_ad`
const windows = `windows`
const alerts = `javascript_alerts`
const dropdown = `dropdown`
const tables = `tables`

module.exports = {
    entryAdd,
    windows,
    alerts,
    dropdown,
    tables,
}